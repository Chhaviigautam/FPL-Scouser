// App.jsx — delegates entirely to the new AppLayout shell.
// All navigation, sidebars, and page routing live in AppLayout.jsx.
import AppLayout from "./AppLayout";

export default function App() {
  return <AppLayout />;
}
